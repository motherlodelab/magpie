# Project memory
Shared facts (both repos) → ~/projects/SHARED-CONTEXT.md (see Memory routing there). Repo-private entries stay in this file.

- User's long-term plan: eventually wrap magpie in a Wails v3 desktop app (new cmd/magpie-desktop binary, per-OS CI builds, Wails is the sanctioned CGO exception) and monetize as BYOK subscription (~$15-25/mo or $149-199/yr, lifetime early-bird) via merchant-of-record (Paddle/LS/Polar) with offline Ed25519 license keys; CLI stays OSS, GUI is the paid product. Positioning: no credits, local-first, zero-LLM verticals as default, MCP-server-as-agent-tool angle. Implication: keep scrape.Deps-style seams and core packages free of terminal coupling so the GUI port stays thin.
- 2026-09-28: User agreed (pushback accepted) — NO Kubernetes/cluster-of-scrapers system for magpie. Product is local-first single-binary; crawling is politeness-bound (per-host 1rps), not compute-bound. If scale is ever needed, climb the ladder: (1) Dockerfile (trivial, CGO-free ~10MB image), (2) docker-compose small farm, (3) K8s Job/CronJob per crawl reusing `crawl --resume`/`watch --once` — no bespoke coordinator; (4) true distributed frontier only for a paid cloud tier, own spec first. Distributed dedup/queue would need Redis = banned new dep without asking.
- magpie LLM setup (2026-09-18): user chose Meta "Muse Spark 1.3 Contributor" via LLM Gateway (OpenAI-compatible, base https://api.llmgateway.io/v1, model slug muse-spark-1.3-contributor). Wired: ~/.config/magpie/config.yaml (extract_provider: openai, model slug), ~/.magpie.env (600, sourced from .bashrc) holds MAGPIE_BASE_URL + MAGPIE_OPENAI_API_KEY, same two env vars injected in project .mcp.json. This WSL box has NO secret service (org.freedesktop.secrets missing) — `magpie config set-key` cannot work here; env-file is the sanctioned fallback. STATUS: endpoint confirmed (their 401 error format) but the pasted token was rejected as not-found — likely copy/truncation or revoked; awaiting replacement key. Privacy note given: Contributor tier trains on usage data. Never store the key itself in memory.
- 2026-09-19: gomagpie renamed to magpie everywhere — repo dir /home/domidex/projects/magpie (old gomagpie path DEAD), GitHub domidex01/magpie (old URLs redirect; Phase V PRs now under motherlodelab/magpie), Go module "magpie", env MAGPIE_*, schema key x-magpie. Historical log entries mentioning "gomagpie" refer to this project. layering-guard-test branch (ef7b9a0) predates the rename — its layering_test.go still says "gomagpie/".
- [insight] go-rod passive event capture gotcha (Phase J): EachEvent callbacks fire ONLY while the returned wait() loop is actively running, and a void callback never satisfies it — calling wait() synchronously hangs until the context dies, and NOT calling it means zero callbacks. Idiom for passive collection: `go wait()` started before navigation, callback mutates a mutex-guarded slice, consumer dies with the page context. Also: proto.NetworkGetResponseBody needs parens in if-init `(proto.X{...}).Call(page)`, and rod's Browser.Close() panics (nil deref) if Connect() failed — nil out the browser field on connect failure so Close() stays a no-op.
